package testFirst;

import static org.junit.Assert.*;

//import java.sql.Date;

import org.junit.Test;

//import auxiliary.InputHelper;

public class TestStart {

	@Test
	public void test() {
//		Start st = new Start();
//		System.out.println(st.toString());
		assertEquals(true, true);
	}
	@Test
	public void testInputhelper() {
/*		Date dt;
		try {
			dt = InputHelper.getSQLDate("En DT");
		} catch (Exception e) {
			dt=Date.valueOf("02.08.2015");
		}
		assertEquals(dt.toString(), new Date(2015, 8, 2).toString() );*/
		assertEquals(true, true);
	}
}
